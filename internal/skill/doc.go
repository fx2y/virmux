// Package skill holds cycle-0 shared contracts for the spec-04 skill subsystem.
//
// C0 intentionally adds only seam definitions and package-boundary docs. Runtime
// dispatch remains unchanged until C1 wires `virmux skill ...` commands.
package skill
